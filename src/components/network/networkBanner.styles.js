import { StyleSheet } from 'react-native';
export const styles = StyleSheet.create({
    banner: {
      backgroundColor: 'red',
      padding: 8,
      alignItems: 'center',
      position: 'absolute',
      bottom: 0,
      left: 0,
      right: 0,
      zIndex: 1000,
    },
    text: { color: 'white' }
  });